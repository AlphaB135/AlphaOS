# security crate

Security orchestrator for sandboxing, manifests, and policy evaluation.

## Week 1-2 Goals
- Define manifest schema describing capability requirements per task.
- Implement policy engine stub that approves display/input/fs servers.
- Integrate with microkernel capability table (enforced via `install_default_manifest`).
