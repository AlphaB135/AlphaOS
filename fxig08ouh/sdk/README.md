# sdk crate

Developer toolkit for writing native AegisOS tasks.
- Re-exports POSIX-style syscall shims.
- Hosts headers under `include/` for C interop.
- Contains examples such as `hello` that render text via display server.

## TODOs
- Publish build scripts for cross-compiling user tasks.
- Add capability manifest generator.
