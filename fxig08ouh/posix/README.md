# posix crate

POSIX surface shim providing syscall numbers and convenience wrappers for native apps.

## Week 1-2 Scope
- Define syscall numbers aligning with `mk::syscalls`.
- Provide thin wrappers (e.g., `write`, `read`, `sleep_ms`).
- Shared with SDK examples for native user tasks.
